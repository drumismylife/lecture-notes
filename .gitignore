# 강의계획서·강의자료 원본(HWP/HWPX)은 로컬 보관 — 웹 레포에 커밋하지 않음
# (내용은 profiles/<과목>/syllabus.md 및 output/.../syllabus.html 로 추출되어 관리됨)
*.hwp
*.hwpx

# 원본 강의 자막/녹취(로컬 보관) — 노트는 output/.../weekNN.html 로 추출되어 관리됨
*자막*.txt
*녹취*.txt
*.vtt
*.srt

# macOS
.DS_Store
